# Sample environment for the chart review platform.
# Copy to `.env` (which is git-ignored) and fill in your own values.
# Loaded automatically by config.py via python-dotenv.

# Provider selection: "azure" (default) or "ollama"
LLM_PROVIDER=azure

# --- Azure OpenAI (required when LLM_PROVIDER=azure) ---
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
# Optional. Defaults to 2024-06-01 if unset.
AZURE_OPENAI_API_VERSION=2024-06-01

# --- Ollama (required when LLM_PROVIDER=ollama) ---
# Install Ollama from https://ollama.com and run: ollama pull <model>
OLLAMA_MODEL=openai/gpt-oss-120b
OLLAMA_BASE_URL=http://localhost:11434
